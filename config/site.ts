export interface SiteConfigType {
  lang: string;
  dir: string;
  siteName: string;
  description: string;
}

const SiteConfig: SiteConfigType = {
  lang: "fa",
  dir: "rtl",
  siteName: "وبی بای",
  description: "وبسایت رویایتو با ما بساز , وبی بای",
};

export default SiteConfig;
